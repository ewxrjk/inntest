# Install prefix for INN
PREFIX=${PREFIX:-/usr/local/news}

# Directory where INN source can be found
DIR=${DIR:-../inn-build}

# Command to elevate privilege
REALLY=${REALLY:-really}

# Test username (also used as group name)
USER=${USER:-tnews}

# Group to use for testing
GROUP=${GROUP:-local.test}

# Name/port of subject server
SERVER=${SERVER:-localhost}
PORT=${PORT:-1119}

# Pathhost of subject server
PATHHOST=${PATHHOST:-test.anjou.terraraq.org.uk}

# Pathhost for test server (and message-ID domain)
DOMAIN=${DOMAIN:-test.terraraq.uk}

# Port for test server
LOCALPORT=${LOCALPORT:-1199}

# Set to -d to enable nntpbits debug output
DEBUG=${DEBUG:-}

# Set to true to run INN and utilities under valgrind
# Output goes ${PREFIX}/log (because writable) - look for filenames like
# 2015-06-21-182422.innd.6762.log
VALGRIND=${VALGRIND:-false}
