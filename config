# Install prefix for INN
PREFIX=/usr/local/news

# Directory where INN source can be found
DIR=../inn-build

# Command to elevate privilege
REALLY=really

# Test username (also used as group name)
USER=tnews

# Group to use for testing
GROUP=greenend.test

# Name/port of subject server
SERVER=localhost
PORT=1119

# Pathhost for test server (and message-ID domain)
DOMAIN=test.anjou.terraraq.org.uk

# Port for test server
LOCALPORT=1199

# Set to -d to enable nntpbits debug output
DEBUG=${DEBUG:-}
